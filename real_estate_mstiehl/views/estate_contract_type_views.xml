<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- View estate.contract.type View List -->
    <record id="view_estate_contract_type_list" model="ir.ui.view">
        <field name="name">view.estate.contract.type.list</field>
        <field name="model">estate.contract.type</field>
        <field name="arch" type="xml">
            <list>
                <field name="name"/>
                <field name="code" />
            </list>
        </field>
    </record>

    <!-- View estate.contract.type form -->
    <record id="view_estate_contract_type_form" model="ir.ui.view">
        <field name="name">view.estate.contract.type.form</field>
        <field name="model">estate.contract.type</field>
        <field name="arch" type="xml">
            <form string="Estate Contract Type">
                <sheet>
                    <group>
                        <field name="name" />
                        <field name="code" />
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <!-- Action estate.contract.type -->
    <record id="estate_contract_type_view_action" model="ir.actions.act_window">
        <field name="name">Estate Contract Type</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">estate.contract.type</field>
        <field name="view_mode">list,form</field>
        <field name="domain">[]</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                There is no examples click here to add new Estate Contract Type.
            </p>
        </field>
    </record>

</odoo>
